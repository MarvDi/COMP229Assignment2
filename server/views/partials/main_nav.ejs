<!-- nav bar-->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <span class="navbar-brand" href="#">
        <img src="Assets\images\logo_transparent.png" alt="" width="30" height="24" class="d-inline-block align-text-top">
        "Marvin" Zhengliang Ding
      </span>
      <!-- toggler -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if (title == "Home"){ %>
            <li class="nav-item active"> 
          <% } else { %>
            <li class="nav-item">
          <% } %>
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/"><i class="fas fa-home"></i> Home</a>
          </li>
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <% if (title == "About"){ %>
              <li class="nav-item active">
              <% } else { %>  
              <li class="nav-item">
              <% } %>
            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="/About"><i class="fas fa-info"></i> About Me</a>
            </li>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <% if (title == "Project"){ %>
                <li class="nav-item active">
                <% } else { %>  
                <li class="nav-item">
                <% } %>
              <li class="nav-item">
                <a class="nav-link" aria-current="page" href="/Project"><i class="far fa-file-archive"></i> Projects</a>
              </li>
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <% if (title == "Services"){ %>
                  <li class="nav-item active">
                  <% } else { %>  
                  <li class="nav-item">
                  <% } %>
                <li class="nav-item">
                  <a class="nav-link" aria-current="page" href="/Services"><i class="far fa-thumbs-up"></i> Services</a>
                </li>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <% if (title == "Contact"){ %>
                    <li class="nav-item active">
                    <% } else { %>  
                    <li class="nav-item">
                    <% } %>
                  <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/Contact"><i class="fas fa-phone-square"></i> Contact Me</a>
                  </li>
                  <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <% if (title == "Login"){ %>
                      <li class="nav-item active"> 
                    <% } else { %>
                      <li class="nav-item">
                    <% } %>
                    <li class="nav-item">
                      <a class="nav-link" aria-current="page" href="/Login"><i class="fas fa-sign-in-alt"></i> Login</a>
                    </li>

                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                      <% if (title == "List"){ %>
                        <li class="nav-item active"> 
                      <% } else { %>
                        <li class="nav-item">
                      <% } %>
                      <li class="nav-item">
                        <a class="nav-link" aria-current="page" href="/contact-list"><i class="fas fa-business-time"></i> Business Contacts</a>
                      </li>

                      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <% if (title == "Update"){ %>
                          <li class="nav-item active"> 
                        <% } else { %>
                          <li class="nav-item">
                        <% } %>
                        <li class="nav-item">
                          <a class="nav-link" aria-current="page" href="/Update"><i class="fas fa-edit"></i> Update</a>
                        </li>
        </ul>
      </div>
    </div>
  </nav>
  <!-- nav bar ends-->